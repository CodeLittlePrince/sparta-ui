import SpButtonGroup from '../button/src/button-group'

/* istanbul ignore next */
SpButtonGroup.install = function(Vue) {
  Vue.component(SpButtonGroup.name, SpButtonGroup)
}

export default SpButtonGroup
